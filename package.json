{
    "scripts": {
        "test": "karma start --single-run",
        "test-watch": "karma start"
    },
    "private": true,
    "dependencies": {
        "bower": "^1.5"
    },
    "devDependencies": {
        "mocha": "^3.3",
        "chai": "^3.5",
        "karma": "^1.7",
        "karma-firefox-launcher": "^1.0.1",
        "karma-mocha": "^1.3.0"
    }
}
